os: linux
dist: bionic
language: bash
notifications:
  email: false
before_script:
- if [[ $TRAVIS_COMMIT_MESSAGE == *"run1"* ]]; then mv deployment1.sh deploy.sh; fi;
deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master
   
   
